[tool.poetry]
name = "pulumitest"
version = "0.1.0"
description = "Python testing framework for Pulumi integration tests"
authors = ["Jonathan Davenport <jdavenport@pulumi.com>"]
readme = "README.md"
packages = [
    { include = "pulumitest" }
]
include = ["pulumitest/py.typed"]

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.4.1"
pulumi = "^3.187.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
